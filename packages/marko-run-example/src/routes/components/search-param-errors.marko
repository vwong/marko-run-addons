<div class="search-param-errors">
  <for|{ name, message }| of=$global.lastQueryErrors>
    <notification level="error">
      ${name} ${message}
    </notification>
  </for>
</div>

style.scss {
  .search-param-errors {
    position: fixed;
    right: $s-16;
    top: $v-20;
  }
}
