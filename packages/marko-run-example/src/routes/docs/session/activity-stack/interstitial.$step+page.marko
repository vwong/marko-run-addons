<layout-doc>
  $ const step = Number($global.params.step);
  <h1>Interstitial ${step}</h1>
  <if(step === 100)>
    <p>This is a dynamically added interstitital.</p>
  </if>
  <form method="POST">
    <csrf-token/>
    <button class="button" type="submit">
      Next
    </button>
  </form>
</layout-doc>

<style-button/>
